yans 外界最按时 GOLD gold 阁楼的

www.yjzs.gold       外界最按时的阁楼

# 一、数据库表设计

## ⭐用户表user（添加了性别）

> 普通用户、管理员用户

user_id

user_nick_name 用户昵称     **唯一字段**

user_img_url 用户头像url

user_sex 用户性别 1为男，0为女，默认为2，表示未知

user_account 用户手机号，用于登录

user_password 密码

user_type 普通用户、后台管理员用户    (0为管理员，1为普通用户)

user_create_time 用户创建时间

user_cancel_time 用户注销时间

user_email 用户邮箱

user_status 用户状态（0为正常，1为删除状态，） 注销操作

user_plate 用户达人值（榜单积分+帖子热度） 当达人值达到10000，开启大V。（暂时不用）

```sql
CREATE TABLE `t_user`
(
    `user_id`          int(11) NOT NULL AUTO_INCREMENT,
    `user_nick_name`   varchar(255) NOT NULL COMMENT ' 用户昵称     **唯一字段**',
    `user_account`     varchar(255) DEFAULT NULL COMMENT ' 用户手机号，用于登录',
    `user_password`    varchar(255) NOT NULL COMMENT ' 密码',
    `user_type`        char(1)      NOT NULL COMMENT ' 普通用户、管理员用户 (0为管理员，1为普通用户),首页只有用户注册，管理员注册得在管理员里面',
    `user_create_time` datetime     NOT NULL,
    `user_cancel_time` datetime     DEFAULT NULL,
    `user_email`       varchar(255) DEFAULT NULL COMMENT ' 用户邮箱',
    `user_status`      char(1)      NOT NULL COMMENT '   用户状态（0为正常，1为删除状态，）',
    `user_plate`       int(11) DEFAULT NULL COMMENT '用户达人值（榜单积分+帖子热度）  当达人值达到10000，开启大V。',
    `user_img_url`     varchar(255) DEFAULT NULL COMMENT '用户头像地址',
    `user_sex`         char(1)      DEFAULT NULL COMMENT '1为男，0为女',
    PRIMARY KEY (`user_id`) USING BTREE,
    UNIQUE KEY `user_nick_name` (`user_nick_name`),
    UNIQUE KEY `user_account` (`user_account`)
) ENGINE=InnoDB AUTO_INCREMENT=2 DEFAULT CHARSET=utf8;
```

## ⭐用户投资信息表info

> 用户登录后，可以个人设置
>
> 默认添加
>
>

info_id

user_id 用户id

info_total_money 用户计划投资总金额 （模拟手头计算投资基金）

info_occupy_money 用户仓位使用金额 （现已投资基金金额+未来收益+未来基金买卖变化）靠算出来的

info_yjzs_profit 入驻系统后累计收益 （+=用户入驻平台后的收益） 算养基表中的基金信息。第二天才算。

info_yjzs_tom_profit 用户昨日收益 （第二题天才显示） 第二天的时候，一起存入redis中,这样来算累计收益。

info_date 建仓时间

```sql
CREATE TABLE `info`
(
    `info_id`              int(11) NOT NULL AUTO_INCREMENT,
    `user_id`              int(11) NOT NULL,
    `info_total_money`     DECIMAL(14, 4) DEFAULT NULL COMMENT '（模拟手头计算投资基金）',
    `info_occupy_money`    DECIMAL(14, 4) NOT NULL COMMENT '用户仓位使用金额       （现已投资基金金额+未来收益+未来基金买卖变化）靠算出来的',
    `info_yjzs_profit`     DECIMAL(14, 4) DEFAULT NULL COMMENT ' 入驻系统后收益  （+=用户入驻平台后的收益）         算养基表中的基金信息。第二天才算。',
    `info_yjzs_tom_profit` DECIMAL(14, 4) DEFAULT NULL COMMENT '用户昨日收益    （第二题天才显示）  第二天的时候，一起存入redis中',
    `info_date`            datetime       DEFAULT NULL,
    PRIMARY KEY (`info_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='用户投资信息表';
```

```
## 用户收益信息表profit

> 用户每日的收益，每个基金对应的收益
>
> 每日收益用函数算出来。

pro_id                         id

user_id                       用户id

fund_id                       基金id

pro_fund_money        一只基金收益金额（这个也要算的）

pro_date                    收益日期            

pro_tyep                    收益类型（0用户累计收益，1基金收益）

pro_total_money       昨日总收益     （type为0的时候，才有，不然为null，是每天早上9点前，开始结算基金收益。）
```

## 用户自选基金表optional （昨日净值和百分率删掉，数据已改，不用）

> 相当于购物车
>
> 看好那些🐏

opt_id 自选id

user_id 用户id

fund_id 基金id

~~fund_name 基金名字~~

~~fund_zrjz 基金昨日净值，~~（**用不上**）

opt_price 自自选时基金交易单位净值，按第二天的算

opt_date 自选日期

~~opt_profit_prop 添加自选后收益率(百分比%)   （当前净值和添加时净值换算） （**用不上**） 暂时不用~~

```sql
CREATE TABLE `optional`
(
    `opt_id`          int(11) NOT NULL AUTO_INCREMENT,
    `user_id`         int(11) NOT NULL,
    `fund_id`         int(11) NOT NULL,
    `fund_zrjz`       decimal(14, 4) DEFAULT NULL COMMENT '基金昨日净值',
    `opt_price`       decimal(14, 4) DEFAULT NULL COMMENT '自自选时基金单位净值，按第二天的算',
    `opt_date`        datetime       DEFAULT NULL COMMENT '自选日期',
    `opt_profit_prop` varchar(255)   DEFAULT NULL COMMENT '添加自选后收益(百分比%)   （当前净值和添加时净值换算）',
    PRIMARY KEY (`opt_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
// 更新
CREATE TABLE `t_optional`
(
    `opt_id`    int(11) NOT NULL AUTO_INCREMENT,
    `user_id`   int(11) NOT NULL,
    `fund_id`   int(11) NOT NULL,
    `opt_price` decimal(14, 4) DEFAULT NULL COMMENT '自自选时基金单位净值，按第二天的算',
    `opt_date`  datetime NOT NULL COMMENT '自选日期，先记录，再根据交易日更新一次。',
    PRIMARY KEY (`opt_id`)
) ENGINE=InnoDB AUTO_INCREMENT=18 DEFAULT CHARSET=utf8;
```

## 用户操作基金表operation

> 把🐏操作买 / 卖
>
> 生成操作贴就是把操作表id拿出去。

ope_id

user_id

fund_id

~~fund_name~~

ope_money 操作基金金额 （这个基金你要买多少钱）**|** （卖出份额得到多少钱）

ope_price 操作基金单价 （根据买 / 卖的时候，获取基金代码，来访问接口获取 **基金净值**）| | 基金购入净值

ope_num 操作基金份额 （第二日，根据净值来算购买了多少份额） 🐏份额不变的   **|**   （卖出的份额）

ope_date 操作日期

ope_status 操作状态(0进行中，1失败，2成功)          记得加到养基表

ope_tepe 操作类型 （0买入还是卖出1，~~2是导入（**名称**、**持有金额**、**持有收益**）~~） |

```sql
CREATE TABLE `operation`
(
    `ope_id`     int(11) NOT NULL AUTO_INCREMENT,
    `user_id`    int(11) NOT NULL,
    `fund_id`    int(11) NOT NULL,
    `ope_money`  decimal(14, 4) DEFAULT NULL COMMENT '操作金额   （这个基金你要买多少钱）**|** （卖出份额得到多少钱）',
    `ope_price`  decimal(14, 4) DEFAULT NULL COMMENT '操作单价  （根据买 / 卖的时候，获取基金代码，来访问接口获取 **基金净值**）  ',
    `ope_num`    decimal(14, 4) DEFAULT NULL COMMENT ' 操作份额     （第二日，根据净值来算购买了多少份额）  ?份额不变的   **|**   （卖出的份额）',
    `ope_date`   datetime NOT NULL COMMENT '操作日期 ',
    `ope_status` char(1)  NOT NULL COMMENT '操作状态(0进行中，1失败，2成功)          记得加到养基表',
    `ope_tepe`   char(1)  NOT NULL COMMENT '（0买入还是卖出1）',
    PRIMARY KEY (`ope_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='用户操作基金表';
```

## ⭐基金收益管理income

> 记录用户id对应基金id的每天收益情况。

inc_id

sup_id 养基id （选取金额不为0的来算）

inc_tom 昨日收益 用份额 x 昨日净值 - 已申购金额 = 昨日收益

inc_data 收益日期

```sql

```

## 用户养基表support （改了基金计划投资金额）

> 用户已经养着的🐏状态

sup_id

user_id

fund_id （去获取基金代码、基金name、基金净值/估算）

~~fund_name 基金名称~~

fund_zrjz 基金昨日净值

sup_money 已申购基金金额 （初始+申购金额+卖出金额+每日收益 ）  *为0就不显示*

sup_price 已申购基金成本 （ 基金金额 / 份额 ） 成本 = 。。。

sup_num 已申购基金份额 （初始+申购份额+卖出份额） 🐏份额不变的 份额 = 。。。

> 用份额 x 昨日净值 - 已申购金额 = 昨日收益

sup_profit 养基累计收益 每个昨日收益之和就是累计收益。 | 持有收益（最新净值 * 份额 - 持有份额 * 持有单）

sup_total_money 基金计划投资金额

suo_date 养基日期

sup_status 养基状态（0，默认正在添加，1 基金金额为0，2成功养鸡） 第二天才养鸡成功

```sql
CREATE TABLE `support`
(
    `sup_id`          int(11) NOT NULL AUTO_INCREMENT,
    `user_id`         int(11) NOT NULL,
    `fund_id`         int(11) NOT NULL COMMENT '（去获取基金代码、基金name、基金净值/估算）',
    `fund_zrjz`       decimal(14, 4) DEFAULT NULL COMMENT '基金昨日净值',
    `sup_money`       decimal(14, 4) DEFAULT NULL COMMENT '（初始+申购金额+卖出金额）  *为0就不显示*',
    `sup_price`       decimal(14, 4) DEFAULT NULL COMMENT '已申购基金成本   （  基金金额  /   份额 ）',
    `sup_num`         decimal(14, 4) DEFAULT NULL COMMENT '已申购基金份额  （初始+申购份额+卖出份额）  ?份额不变的',
    `sup_profit`      decimal(14, 4) DEFAULT NULL COMMENT '养基收益（基金金额    -  （成本净值*份额））',
    `sup_total_money` decimal(14, 4) DEFAULT NULL COMMENT '基金仓位金额',
    `suo_date`        datetime       DEFAULT NULL COMMENT '养基日期',
    `sup_status`      char(1) NOT NULL COMMENT ' 养基状态（0，默认正在添加，1 基金金额为0，2成功养鸡） 第二天才养鸡成功',
    PRIMARY KEY (`sup_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='用户养基表';
```

```java
/*
用户基金卖出表sell

> 把🐏卖出去

sell_id

user_id                    ⭐每次调用外键，要检查外键是否存在，不存在就删除记录

fund_id                          

sell_num  卖出份额           🐏份额不变的

sell_price   卖出单价      （第二天知道）

sell_momey  卖出金额       （份额 * 单价） 

sell_status 卖出状态   （0进行中，1失败，2成功）   记得去仓位扣点基金前

sell_date   卖出时间   （第二题再改状态）
*/


```

## ⭐基金信息表fund

> 基金代码

fund_id

fund_code 基金代码

fund_name 基金名称

fund_status 基金状态 （0为正常，1为删除状态，2为待确认状态）

```sql
CREATE TABLE `fund`
(
    `fund_id`     int(11) NOT NULL AUTO_INCREMENT,
    `fund_code`   varchar(16) NOT NULL COMMENT '  基金代码',
    `fund_name`   varchar(32) NOT NULL COMMENT ' 基金名称',
    `fund_status` char(1)     NOT NULL COMMENT '基金状态   （0为正常，1为删除状态，2为待确认状态）',
    PRIMARY KEY (`fund_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

## ⭐帖子表posts

> 也有删除功能，只不过标识为永久删除和普通封禁。

pos_id 帖子表

user_id ⭐每次调用外键，要检查外键是否存在，不存在就删除记录

~~user_nick_name 用户昵称~~

pos_title 帖子标题

pos_content 帖子内容

pos_date 帖子日期

pos_hot 帖子热度（点赞+1，评论+5，收藏的话会额外+10） ⭐ **获取赞得算：帖子热度 - 被收藏数X10 - 评论数*X 5 = 点赞数**⭐

pos_status 帖子状态（0为正常，1为删除状态 ） 用户只能改状态，管理员可以删除

pos_type  (0公开，1私有，~~2为公告~~)

pos_off 举报状态（默认0正常，1为嫌疑，2为举报成功） 可以设置状态为2的次数太多了比如20次，就强制注销账户。

```sql
CREATE TABLE `posts`
(
    `pos_id`      int(11) NOT NULL AUTO_INCREMENT,
    `user_id`     int(11) NOT NULL,
    `pos_title`   varchar(16)  NOT NULL COMMENT '帖子标题',
    `pos_content` varchar(255) NOT NULL COMMENT '帖子内容',
    `pos_date`    datetime     NOT NULL COMMENT '帖子日期',
    `pos_hot`     int(11) NOT NULL COMMENT '帖子热度（点赞，收藏的话会额外+10）',
    `pos_status`  char(1)      NOT NULL COMMENT '帖子状态（0为正常，1为删除状态 ）   用户只能改状态，管理员可以删除',
    `pos_type`    char(1)      NOT NULL COMMENT '(0公开，1私有)',
    `pos_off`     char(1)      NOT NULL COMMENT '(0默认，1审核，2违规)',
    PRIMARY KEY (`pos_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

## 评论表comment （添加了帖子id）(comType的char1改为了varchar16)

> 评论帖子

com_id 评论id

user_id 你自己的id

⭐pos_id 帖子id （这修改了）

com_con_id 关联id （主评论id），表示一个帖子评论id ⭐每次调用外键，要检查外键是否存在，不存在就删除记录

com_content 评论内容

com_date 评论时间

com_type 评论类型(0评论帖子，1回复帖子评论，2回复评论的帖子评论)

评论帖子时：只需要看是否是 帖子id、评论类型（0）。

评论帖子评论时：需要看 帖子id、评论类型（1）、关联id（表示一个主评论）

评论评论用户的评论：需要 帖子id、帖子评论id、评论类型（“@”+“用户id” , “@2”）、关联id（表示主评论）

```sql
CREATE TABLE `t_comment`
(
    `com_id`      int(11) NOT NULL AUTO_INCREMENT,
    `user_id`     int(11) NOT NULL,
    `com_con_id`  int(11) DEFAULT NULL COMMENT '关联id    （0时为null）（1时为评论id）',
    `com_content` varchar(255) NOT NULL COMMENT '评论内容',
    `com_date`    datetime     NOT NULL COMMENT '评论时间',
    `com_type`    char(1)      NOT NULL COMMENT '评论类型(0评论帖子，1回复评论)',
    `pos_id`      int(11) NOT NULL,
    PRIMARY KEY (`com_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

## 关注表follow

> start为达人，fans为粉丝

foll_id

start_user_id

~~start_user_nick~~

fans_user_id

~~fans_user_nick~~

foll_date 关注时间

```sql
CREATE TABLE `follow`
(
    `foll_id`       int(11) NOT NULL AUTO_INCREMENT,
    `start_user_id` int(11) NOT NULL COMMENT '达人id',
    `fans_user_id`  int(11) NOT NULL COMMENT '用户id',
    `foll_date`     datetime NOT NULL COMMENT '关注时间',
    PRIMARY KEY (`foll_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

## 收藏表collect

> 将帖子收藏

coll_id

pos_id 帖子表

~~pos_title 帖子标题~~

user_id

~~user_nick_name 被收藏者昵称~~

coll_date 收藏时间

```sql
CREATE TABLE `collect`
(
    `coll_id`   int(11) NOT NULL AUTO_INCREMENT,
    `pos_id`    int(11) NOT NULL COMMENT '帖子表id',
    `user_id`   int(11) NOT NULL COMMENT '用户id',
    `coll_date` datetime NOT NULL COMMENT '收藏时间',
    PRIMARY KEY (`coll_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

## 点赞表like（新建）

> 记录点赞的信息

like_id 点赞id

user_id 点赞人id

pos_id 点赞的帖子id

like_date 点赞时间

```sql
CREATE TABLE `t_like`
(
    `like_id`   int(11) NOT NULL COMMENT '点赞id',
    `user_id`   int(11) NOT NULL COMMENT '点赞用户id',
    `pos_id`    int(11) NOT NULL COMMENT '点赞帖子id',
    `like_date` datetime NOT NULL COMMENT '点赞时间',
    PRIMARY KEY (`like_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

## ⭐创建表SQL语句

```sql
CREATE TABLE `user`
(
    `user_id`          int(11) NOT NULL AUTO_INCREMENT,
    `user_nick_name`   varchar(255) NOT NULL COMMENT ' 用户昵称     **唯一字段**',
    `user_account`     varchar(255) NOT NULL COMMENT ' 用户手机号，用于登录',
    `user_password`    char(255)    NOT NULL COMMENT ' 密码',
    `user_type`        char(1)      NOT NULL COMMENT ' 普通用户、管理员用户 (0为管理员，1为普通用户),首页只有用户注册，管理员注册得在管理员里面',
    `user_create_time` datetime     NOT NULL,
    `user_cancel_time` datetime DEFAULT NULL,
    `user_email`       varchar(255) NOT NULL COMMENT ' 用户邮箱',
    `user_status`      char(1)      NOT NULL COMMENT '   用户状态（0为正常，1为删除状态，）',
    `user_plate`       int(11) DEFAULT NULL COMMENT '用户达人值（榜单积分+帖子热度）  当达人值达到10000，开启大V。',
    `user_img_url`     varchar(255) NOT NULL COMMENT '用户头像地址',
    PRIMARY KEY (`user_id`) USING BTREE,
    UNIQUE KEY `user_nick_name` (`user_nick_name`),
    UNIQUE KEY `user_account` (`user_account`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

CREATE TABLE `info`
(
    `info_id`              int(11) NOT NULL AUTO_INCREMENT,
    `user_id`              int(11) NOT NULL,
    `info_total_money`     DECIMAL(14, 4) DEFAULT NULL COMMENT '（模拟手头计算投资基金）',
    `info_occupy_money`    DECIMAL(14, 4) NOT NULL COMMENT '用户仓位使用金额       （现已投资基金金额+未来收益+未来基金买卖变化）靠算出来的',
    `info_yjzs_profit`     DECIMAL(14, 4) DEFAULT NULL COMMENT ' 入驻系统后收益  （+=用户入驻平台后的收益）         算养基表中的基金信息。第二天才算。',
    `info_yjzs_tom_profit` DECIMAL(14, 4) DEFAULT NULL COMMENT '用户昨日收益    （第二题天才显示）  第二天的时候，一起存入redis中',
    `info_date`            datetime       DEFAULT NULL,
    PRIMARY KEY (`info_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='用户投资信息表';

CREATE TABLE `optional`
(
    `opt_id`          int(11) NOT NULL AUTO_INCREMENT,
    `user_id`         int(11) NOT NULL,
    `fund_id`         int(11) NOT NULL,
    `fund_zrjz`       decimal(14, 4) DEFAULT NULL COMMENT '基金昨日净值',
    `opt_price`       decimal(14, 4) DEFAULT NULL COMMENT '自自选时基金单位净值，按第二天的算',
    `opt_date`        datetime       DEFAULT NULL COMMENT '自选日期',
    `opt_profit_prop` varchar(255)   DEFAULT NULL COMMENT '添加自选后收益(百分比%)   （当前净值和添加时净值换算）',
    PRIMARY KEY (`opt_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

CREATE TABLE `operation`
(
    `ope_id`     int(11) NOT NULL AUTO_INCREMENT,
    `user_id`    int(11) NOT NULL,
    `fund_id`    int(11) NOT NULL,
    `ope_money`  decimal(14, 4) DEFAULT NULL COMMENT '操作金额   （这个基金你要买多少钱）**|** （卖出份额得到多少钱）',
    `ope_price`  decimal(14, 4) DEFAULT NULL COMMENT '操作单价  （根据买 / 卖的时候，获取基金代码，来访问接口获取 **基金净值**）  ',
    `ope_num`    decimal(14, 4) DEFAULT NULL COMMENT ' 操作份额     （第二日，根据净值来算购买了多少份额）  ?份额不变的   **|**   （卖出的份额）',
    `ope_date`   datetime NOT NULL COMMENT '操作日期 ',
    `ope_status` char(1)  NOT NULL COMMENT '操作状态(0进行中，1失败，2成功)          记得加到养基表',
    `ope_tepe`   char(1)  NOT NULL COMMENT '（0买入还是卖出1）',
    PRIMARY KEY (`ope_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='用户操作基金表';

CREATE TABLE `support`
(
    `sup_id`          int(11) NOT NULL AUTO_INCREMENT,
    `user_id`         int(11) NOT NULL,
    `fund_id`         int(11) NOT NULL COMMENT '（去获取基金代码、基金name、基金净值/估算）',
    `fund_zrjz`       decimal(14, 4) DEFAULT NULL COMMENT '基金昨日净值',
    `sup_money`       decimal(14, 4) DEFAULT NULL COMMENT '（初始+申购金额+卖出金额）  *为0就不显示*',
    `sup_price`       decimal(14, 4) DEFAULT NULL COMMENT '已申购基金成本   （  基金金额  /   份额 ）',
    `sup_num`         decimal(14, 4) DEFAULT NULL COMMENT '已申购基金份额  （初始+申购份额+卖出份额）  ?份额不变的',
    `sup_profit`      decimal(14, 4) DEFAULT NULL COMMENT '养基收益（基金金额    -  （成本净值*份额））',
    `sup_total_money` decimal(14, 4) DEFAULT NULL COMMENT '基金仓位金额',
    `suo_date`        datetime       DEFAULT NULL COMMENT '养基日期',
    `sup_status`      char(1) NOT NULL COMMENT ' 养基状态（0，默认正在添加，1 基金金额为0，2成功养鸡） 第二天才养鸡成功',
    PRIMARY KEY (`sup_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='用户养基表';

CREATE TABLE `fund`
(
    `fund_id`     int(11) NOT NULL AUTO_INCREMENT,
    `fund_code`   varchar(16) NOT NULL COMMENT '  基金代码',
    `fund_name`   varchar(32) NOT NULL COMMENT ' 基金名称',
    `fund_status` char(1)     NOT NULL COMMENT '基金状态   （0为正常，1为删除状态，2为待确认状态）',
    PRIMARY KEY (`fund_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

CREATE TABLE `posts`
(
    `pos_id`      int(11) NOT NULL AUTO_INCREMENT,
    `user_id`     int(11) NOT NULL,
    `pos_title`   varchar(16)  NOT NULL COMMENT '帖子标题',
    `pos_content` varchar(255) NOT NULL COMMENT '帖子内容',
    `pos_date`    datetime     NOT NULL COMMENT '帖子日期',
    `pos_hot`     int(11) NOT NULL COMMENT '帖子热度（点赞，收藏的话会额外+10）',
    `pos_status`  char(1)      NOT NULL COMMENT '帖子状态（0为正常，1为删除状态 ）   用户只能改状态，管理员可以删除',
    `pos_type`    char(1)      NOT NULL COMMENT '(0公开，1私有)',
    `pos_off`     char(1)      NOT NULL COMMENT '(0默认，1审核，2违规)',
    PRIMARY KEY (`pos_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

CREATE TABLE `comment`
(
    `com_id`      int(11) NOT NULL AUTO_INCREMENT,
    `user_id`     int(11) NOT NULL,
    `com_con_id`  int(11) NOT NULL COMMENT '关联id    （0时为帖子id）（1时为评论id）',
    `com_content` varchar(255) NOT NULL COMMENT '评论内容',
    `com_date`    datetime     NOT NULL COMMENT '评论时间',
    `com_type`    char(1)      NOT NULL COMMENT '评论类型(0评论帖子，1回复评论)',
    PRIMARY KEY (`com_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

CREATE TABLE `follow`
(
    `foll_id`       int(11) NOT NULL AUTO_INCREMENT,
    `start_user_id` int(11) NOT NULL COMMENT '达人id',
    `fans_user_id`  int(11) NOT NULL COMMENT '用户id',
    `foll_date`     datetime NOT NULL COMMENT '关注时间',
    PRIMARY KEY (`foll_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

CREATE TABLE `collect`
(
    `coll_id`   int(11) NOT NULL AUTO_INCREMENT,
    `pos_id`    int(11) NOT NULL COMMENT '帖子表id',
    `user_id`   int(11) NOT NULL COMMENT '用户id',
    `coll_date` datetime NOT NULL COMMENT '收藏时间',
    PRIMARY KEY (`coll_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

****

# 二、功能模块设计

## 1.选基模块

> 根据用户的输入，模糊查询，调用其接口，再根据获取的部分信息，提供相关提示。

## 2.基场模块

> 红绿深浅展示基金的涨幅，进行基金申购，生成提醒操作信息。

## 3.榜单模块

> 一些信息的定榜，激励用户学习理财。

## 4.社区模块

> 评论、发帖、管理员对帖子管理等操作。

## 5.用户模块

> 个人信息管理、仓位设置、管理员对用户管理等操作。

## 6.基金模块

> 管理员对可以管理的基金进行信息管理。







****

# 三、用户界面模块

## 1、首页

> 关注点：预估收益

> 包含功能模块：选基、

## 2、选基

> 关注点：查询基金
>
> 包含功能模块：选基

## 3、基场

> 关注点：投资的基金

> 包含功能模块：选基、基场

## 4、社区

> 关注点：帖子

> 包含功能模块：社区

## 5、榜单

> 关注点：排行

> 包含功能模块：榜单

## 6、我的

> 关注点：个人信息

> 包含功能模块：用户

# 四、后台界面模块

## 1、首页

> 展示平台数据

## 2、社区

> 展示社区交流信息，**私密信息不可检阅**。

## 4、基金

> 展示入驻平台基金信息

## 5、用户

> 展示用户信息

## 6、我的

> 个人信息





****

# 五、微服务的划分

## 1、用户微服务yjzs_user

### 1.0、数据库表

#### 1.0.1、用户表

```sql
CREATE TABLE `user`
(
    `user_id`          int(11) NOT NULL AUTO_INCREMENT,
    `user_nick_name`   varchar(255) NOT NULL COMMENT ' 用户昵称     **唯一字段**',
    `user_account`     varchar(255) NOT NULL COMMENT ' 用户手机号，用于登录',
    `user_password`    varchar(255) NOT NULL COMMENT ' 密码',
    `user_type`        char(1)      NOT NULL COMMENT ' 普通用户、管理员用户 (0为管理员，1为普通用户),首页只有用户注册，管理员注册得在管理员里面',
    `user_create_time` datetime     NOT NULL,
    `user_cancel_time` datetime DEFAULT NULL,
    `user_email`       varchar(255) NOT NULL COMMENT ' 用户邮箱',
    `user_status`      char(1)      NOT NULL COMMENT '   用户状态（0为正常，1为删除状态，）',
    `user_plate`       int(11) DEFAULT NULL COMMENT '用户达人值（榜单积分+帖子热度）  当达人值达到10000，开启大V。',
    `user_img_url`     varchar(255) NOT NULL COMMENT '用户头像地址',
    PRIMARY KEY (`user_id`) USING BTREE,
    UNIQUE KEY `user_nick_name` (`user_nick_name`),
    UNIQUE KEY `user_account` (`user_account`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

#### 1.0.2、投资信息表

```sql
CREATE TABLE `info`
(
    `info_id`              int(11) NOT NULL AUTO_INCREMENT,
    `user_id`              int(11) NOT NULL,
    `info_total_money`     decimal(14, 4) DEFAULT NULL COMMENT '（模拟手头计算投资基金）',
    `info_occupy_money`    decimal(14, 4) DEFAULT NULL COMMENT '用户仓位使用金额       （现已投资基金金额+未来收益+未来基金买卖变化）靠算出来的',
    `info_yjzs_profit`     decimal(14, 4) DEFAULT NULL COMMENT ' 入驻系统后收益  （+=用户入驻平台后的收益）         算养基表中的基金信息。第二天才算。',
    `info_yjzs_tom_profit` decimal(14, 4) DEFAULT NULL COMMENT '用户昨日收益    （第二题天才显示）  第二天的时候，一起存入redis中',
    `info_date`            datetime       DEFAULT NULL COMMENT '建仓时间',
    PRIMARY KEY (`info_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='用户投资信息表';
```

### 1.1、用户模块

>

### 1.2、榜单模块

> 一般显示前10，每天刷新一次。
>
> ①用户昨日收益榜单 （**多**）      **用户昵称、用户收益（在前端最好别显示数字，用物品代替）**
>
> **昨日收益**            前十，依次加100、90、80.。。。累计10000分，获得大V
>
> ②用户入驻系统后收益榜单（**多**）       **用户昵称、用户收益（在前端最好别显示数字，用物品代替）**
>
>  **累计收益**         前十，依次加100、90、80.。。。累计10000分，获得大V
>
> ③用户收益率榜单（**多/少**） 镰刀/韭菜 榜单      **用户昵称、用户收益率（持有收益 / 持有成本 * 100%*）**
>
> **累计收益率**        （累计收益/投资成本） 前十，依次加分1000、900、800.。。。累计10000分，获得大V称号
>
> ④累计亏损率
>
> **累计亏损率**        （累计收益/投资成本） 前十，依次扣分1000、900、800.。。。累计10000分，
>
> ⑤平台热门基金榜单 （**多**）持有的人数       **基金名称、持有人数 ** （无加分）
>
> ⑥人气达人排行 ，热度最高的前10，不加分。

> 先用sql查出来。返回用户id的数组，再来遍历加分，再放到redis去。

## 2、养基微服务yjzs_main

### 2.0、数据库表

#### 2.0.1、养基表

```sql
CREATE TABLE `support`
(
    `sup_id`          int(11) NOT NULL AUTO_INCREMENT,
    `user_id`         int(11) NOT NULL,
    `fund_id`         int(11) NOT NULL COMMENT '（去获取基金代码、基金name、基金净值/估算）',
    `fund_zrjz`       decimal(14, 4) DEFAULT NULL COMMENT '基金昨日净值',
    `sup_money`       decimal(14, 4) DEFAULT NULL COMMENT '（初始+申购金额+卖出金额）  *为0就不显示*',
    `sup_price`       decimal(14, 4) DEFAULT NULL COMMENT '已申购基金成本   （  基金金额  /   份额 ）',
    `sup_num`         decimal(14, 4) DEFAULT NULL COMMENT '已申购基金份额  （初始+申购份额+卖出份额）  ?份额不变的',
    `sup_profit`      decimal(14, 4) DEFAULT NULL COMMENT '养基收益（基金金额    -  （成本净值*份额））',
    `sup_total_money` decimal(14, 4) DEFAULT NULL COMMENT '基金仓位金额',
    `suo_date`        datetime NOT NULL COMMENT '养基日期',
    `sup_status`      char(1)  NOT NULL COMMENT ' 养基状态（0，默认正在添加，1 基金金额为0，2成功养鸡） 第二天才养鸡成功',
    PRIMARY KEY (`sup_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='用户养基表';
```

#### 2.0.2、操作表

```sql
CREATE TABLE `operation`
(
    `ope_id`     int(11) NOT NULL AUTO_INCREMENT,
    `user_id`    int(11) NOT NULL,
    `fund_id`    int(11) NOT NULL,
    `ope_money`  decimal(14, 4) DEFAULT NULL COMMENT '操作金额   （这个基金你要买多少钱）**|** （卖出份额得到多少钱）',
    `ope_price`  decimal(14, 4) DEFAULT NULL COMMENT '操作单价  （根据买 / 卖的时候，获取基金代码，来访问接口获取 **基金净值**）  ',
    `ope_num`    decimal(14, 4) DEFAULT NULL COMMENT ' 操作份额     （第二日，根据净值来算购买了多少份额）  ?份额不变的   **|**   （卖出的份额）',
    `ope_date`   datetime NOT NULL COMMENT '操作日期 ',
    `ope_status` char(1)  NOT NULL COMMENT '操作状态(0进行中，1失败，2成功)          记得加到养基表',
    `ope_tepe`   char(1)  NOT NULL COMMENT '（0买入还是卖出1）',
    PRIMARY KEY (`ope_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='用户操作基金表';
```

#### 2.0.3、基金信息表

```sql
CREATE TABLE `fund`
(
    `fund_id`     int(11) NOT NULL AUTO_INCREMENT,
    `fund_code`   varchar(16) NOT NULL COMMENT '  基金代码',
    `fund_name`   varchar(32) NOT NULL COMMENT ' 基金名称',
    `fund_status` char(1)     NOT NULL COMMENT '基金状态   （0为正常，1为删除状态，2为待确认状态）',
    PRIMARY KEY (`fund_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

#### 2.0.4、自选基金表

```sql
CREATE TABLE `optional`
(
    `opt_id`          int(11) NOT NULL AUTO_INCREMENT,
    `user_id`         int(11) NOT NULL,
    `fund_id`         int(11) NOT NULL,
    `fund_zrjz`       decimal(14, 4) DEFAULT NULL COMMENT '基金昨日净值',
    `opt_price`       decimal(14, 4) DEFAULT NULL COMMENT '自自选时基金单位净值，按第二天的算',
    `opt_date`        datetime NOT NULL COMMENT '自选日期',
    `opt_profit_prop` varchar(8)     DEFAULT NULL COMMENT '添加自选后收益(百分比%)   （当前净值和添加时净值换算）',
    PRIMARY KEY (`opt_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

### 2.1、选基模块

>

### 2.2、基场模块

>

### 2.3、基金管理

>

## 3、社区微服务yjzs_com

### 3.0、数据库表

#### 3.0.1、帖子表

```sql
CREATE TABLE `posts`
(
    `pos_id`      int(11) NOT NULL AUTO_INCREMENT,
    `user_id`     int(11) NOT NULL,
    `pos_title`   varchar(16)  NOT NULL COMMENT '帖子标题',
    `pos_content` varchar(255) NOT NULL COMMENT '帖子内容',
    `pos_date`    datetime     NOT NULL COMMENT '帖子日期',
    `pos_hot`     int(11) NOT NULL COMMENT '帖子热度（点赞，收藏的话会额外+10）',
    `pos_status`  char(1)      NOT NULL COMMENT '帖子状态（0为正常，1为删除状态 ）   用户只能改状态，管理员可以删除',
    `pos_type`    char(1)      NOT NULL COMMENT '(0公开，1私有)',
    `pos_off`     char(1)      NOT NULL COMMENT '(0默认，1审核，2违规)',
    PRIMARY KEY (`pos_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

#### 3.0.2、评论表

```sql
CREATE TABLE `comment`
(
    `com_id`      int(11) NOT NULL AUTO_INCREMENT,
    `user_id`     int(11) NOT NULL,
    `com_con_id`  int(11) NOT NULL COMMENT '关联id    （0时为帖子id）（1时为评论id）',
    `com_content` varchar(255) NOT NULL COMMENT '评论内容',
    `com_date`    datetime     NOT NULL COMMENT '评论时间',
    `com_type`    char(1)      NOT NULL COMMENT '评论类型(0评论帖子，1回复评论)',
    PRIMARY KEY (`com_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

#### 3.0.3、关注表

```sql
CREATE TABLE `follow`
(
    `foll_id`       int(11) NOT NULL AUTO_INCREMENT,
    `start_user_id` int(11) NOT NULL COMMENT '达人id',
    `fans_user_id`  int(11) NOT NULL COMMENT '用户id',
    `foll_date`     datetime NOT NULL COMMENT '关注时间',
    PRIMARY KEY (`foll_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

#### 3.0.4、收藏表

```sql
CREATE TABLE `collect`
(
    `coll_id`   int(11) NOT NULL AUTO_INCREMENT,
    `pos_id`    int(11) NOT NULL COMMENT '帖子表id',
    `user_id`   int(11) NOT NULL COMMENT '用户id',
    `coll_date` datetime NOT NULL COMMENT '收藏时间',
    PRIMARY KEY (`coll_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

### 3.1、社区模块

> 





